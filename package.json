{
  "name": "ada",
  "private": true,
  "description": "Playwright tests for the ada alert page (alert.html)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
